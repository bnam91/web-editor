# Design FIX Plan — 2026-06-11

design-squad FIX 모드. **승인 범위: blocker 저대비 2건만** (`--ui-text-muted` #777 / `--ui-text-dim` #555, AA 미달).
현빈 컨펌(2026-06-11). 승인 범위 밖(토큰 이원화·focus-visible·reduced-motion 등 major)은 **불가침**.
커밋 안 함 — diff 요약 + 재실측 보고까지.

## 사용처 분류 (①단계 결과)
| 토큰 | 텍스트(color) | 비텍스트(border/bg/장식점) |
|------|:---:|:---:|
| `--ui-text-dim` #555 | 87곳 | **19곳** (hover 보더 14, 점선 보더 3, 토글 knob·prop dot 2) |
| `--ui-text-muted` #777 | 37곳 | **2곳** (autosave 점멸 점, cpm 연결안됨 status dot) |

→ **텍스트/비텍스트 혼용 확인.** 토큰 값만 올리면 21곳 보더·장식점이 같이 밝아져 시각 회귀. **토큰 분리 필요.**
비텍스트 21곳은 전부 "비활성/구분/장식" 의미(연결안됨 dot, 저장중 dot, OFF 토글 knob, hover 강조 보더) — 낮은 값이 의미상 맞음. → `--ui-divider`로 분리해 현재 #555 외형 보존.

## 목표값 (CDP 대비율 계산 — 모든 셸 배경 #1a~#2e에서 AA 4.5:1 통과)
| 토큰 | 현재 | → 변경 | #2a | #2e(hover) |
|------|------|--------|-----|-----------|
| `--ui-text-dim` (텍스트) | #555 (1.93) | **#969696** | 4.85 | 4.59 ✅ |
| `--ui-text-muted` (텍스트) | #777 (3.21) | **#9e9e9e** | 5.36 | 5.07 ✅ |
| `--ui-divider` (신설, 비텍스트) | — | **#555555** | (장식·보더, 대비 무관) |

티어 순서 보존: `--ui-text-sub` #aaa(170) > muted #9e(158) > dim #96(150). 새 hex 없음(divider=기존 dim값 이전).

---

## PR-1: 비텍스트 역할 토큰 분리 (`--ui-divider` 신설 + 21곳 재지정) — **시각 무변화 리팩터**
- **해결 이슈**: (PR-2의 전제 — 분리 없이 raise하면 보더/장식 깨짐)
- **수정 범위**:
  - `css/editor-base.css` :root에 `--ui-divider: #555555;` 추가 (1줄)
  - 비텍스트 21곳을 `var(--ui-text-dim/muted)` → `var(--ui-divider)` 재지정:
    - dim 19곳: editor-blocks:389 / editor-base:631,638,655 / editor-panels:642,763,1566,1652,1685 / editor-props:199,214,254,269,282,476,484,495,527 / editor-layout:430
    - muted 2곳: claude-pm:120(.cpm-status-dot) / editor-base:755(autosave saving dot)
- **변경 이유**: 텍스트 토큰을 raise해도 보더/장식점이 따라 밝아지지 않게 역할 분리.
- **검증법**: divider=#555=기존 dim/muted-bg와 동일값이므로 **스크린샷 before/after 동일해야 함**(픽셀 무변화). cpm dot·autosave dot은 #777→#555로 미세 dim(비활성 상태라 의미상 OK, 위험에 기재).
- **롤백**: 단일 커밋 revert. 21곳 sed 역치환.
- **리스크**: 낮음. cpm-status-dot/autosave-saving-dot 2곳만 #777→#555 미세 어두워짐(둘 다 "비활성/진행중" 점이라 수용). 나머지 19곳은 완전 동일.

## PR-2: 텍스트 토큰 AA 상향 (`--ui-text-dim`/`--ui-text-muted` 값 변경) — **실제 픽스**
- **해결 이슈**: DSN-a11y-contrast-01(#555), -03(#777) blocker
- **수정 범위**: `css/editor-base.css` :root 2줄 — `--ui-text-dim: #555→#969696`, `--ui-text-muted: #777→#9e9e9e`. (design-tokens.css의 `--text-hint`/dead 의미토큰은 승인범위 밖 → 미수정)
- **변경 이유**: 텍스트 124곳(dim 87 + muted 37) 일괄 AA 통과.
- **검증법**: 수정 후 CDP 렌더 재실측 — `.section-label`/`.tb-badge` 등 실제 요소 대비율 재계산(목표 ≥4.5) + before/after 스크린샷.
- **롤백**: 2줄 revert.
- **리스크**: 중립색 124곳이 밝아짐(의도된 가독성 개선). 텍스트 위계가 살짝 압축(muted/dim 간격 #777↔#555 → #9e↔#96)되나 둘 다 원래 AA 미달이라 수용. 캔버스 콘텐츠는 `--preset-*`라 영향 없음(불가침 보존).

순서: PR-1(무변화 분리) → PR-2(상향). PR-1이 선행돼야 PR-2가 보더를 안 건드림.
