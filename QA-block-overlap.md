# 블록 겹침(pullUp) — 라이브 QA 기록 (2026-09-04)

대상: `feat/block-overlap` @ `90049a9` · 시험 프로젝트 `proj_1788435202695`(전용 스크래치)
방법: 앱 실행 + CDP 실측. ★서브에이전트는 전원 읽기전용(앱 실행·CDP 접속 금지)으로 격리.

| # | 항목 | 결과 | 실측값 |
|---|---|---|---|
| 1 | 겹침 적용 | ✅ | 텍스트 단위 469→421px(줌 40%, 캔버스 −120px 정확) |
| 2 | 저장·재로드 | ✅ | `-140px` 재로드 후 그대로. 내보내기 코드 0줄 수정 |
| 3 | 페인트 순서 | ✅ | 배경 있는 단위를 위로 당겨도 아래 글자가 안 뚫림(수정 전엔 뚫렸음, 스크린샷 대조) |
| 4 | 상단 클램프 | ✅ | 두번째 단위 −600 요청 → **−100 으로 잘림**, 단위 상단 = 섹션 상단(84=84) |
| 5 | ⌥↑ / ⌥↓ | ✅ | −120→−122(1px), ⌥⇧↓ −122→−112(10px) |
| 6 | 섹션 일괄 해제 | ✅ | 「이 섹션 겹침 해제 (1개)」 → pullUp 0, 인라인 빔 |
| 7 | ⌥클릭(겹친 아래 선택) | ✅ | 겹친 지점에서 text-block ↔ asset-block **순환** |
| 8 | 드롭 대상 도달성 | ✅ | pull 0/−60/−140/−260/−400 전부 **7/7 도달, 도달불가 0** |
| 9 | undo / redo | ✅ | −180px → undo 빔 → redo −180px |
| 10 | ⌘↑ 순서 이동 | ⚪ | 합성 호출로는 미동작 — **겹침 0에서도 동일** ⇒ 회귀 아님(선택 판정 문제) |

## 미해결 — 현빈 결정 필요
**Figma JSON · Design JSON 은 겹침을 싣지 못한다.** `export-figma-json.js:889` 가 저장 문자열을
DOMParser 로 «오프라인 파싱»해 순회하고 섹션 객체가 `{id,name,background,bgImage,blocks}` 뿐이라
y·height 축이 없다(:881). z-index·marginTop 참조 각각 0회. Design JSON 도 의미 모델이라 좌표축 없음.
⇒ ⒜「화면·저장·HTML·PNG 전용, JSON 2종은 겹침 없는 근사」로 명시하고 간다  또는
  ⒝ 스키마에 pullUp 축 추가 + Figma 플러그인 빌더 수정(별건).

## 잠복 지뢰(이번 건 무관, 싸게 막을 수 있음)
`banner02-block.js:106` · `comparison-block.js:151` 이 `block.style.margin` **단축속성**을 쓴다.
지금은 두 블록이 항상 `.row` 로 감싸여 겹침 단위가 row 라 안 터지지만, 래핑이 깨지면
저장과 PNG 가 «동시에» 조용히 무너진다. `marginLeft/marginRight` 로 바꿔두는 게 싸다.
