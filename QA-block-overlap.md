# 블록 겹침(pullUp) — 라이브 QA 기록 (2026-09-04)

대상: `feat/block-overlap` @ `90049a9` · 시험 프로젝트 `proj_1788435202695`(전용 스크래치)
방법: 앱 실행 + CDP 실측. ★서브에이전트는 전원 읽기전용(앱 실행·CDP 접속 금지)으로 격리.

| # | 항목 | 결과 | 실측값 |
|---|---|---|---|
| 1 | 겹침 적용 | ✅ | 텍스트 단위 469→421px(줌 40%, 캔버스 −120px 정확) |
| 2 | 저장·재로드 | ✅ | `-140px` 재로드 후 그대로. 내보내기 코드 0줄 수정 |
| 3 | 페인트 순서 | ✅ | 배경 있는 단위를 위로 당겨도 아래 글자가 안 뚫림(수정 전엔 뚫렸음, 스크린샷 대조) |
| 4 | 상단 클램프 | ✅ | 두번째 단위 −600 요청 → **−100 으로 잘림**, 단위 상단 = 섹션 상단(84=84) |
| 5 | ⌥↑ / ⌥↓ | ✅ | −120→−122(1px), ⌥⇧↓ −122→−112(10px) |
| 6 | 섹션 일괄 해제 | ✅ | 「이 섹션 겹침 해제 (1개)」 → pullUp 0, 인라인 빔 |
| 7 | ⌥클릭(겹친 아래 선택) | ✅ | 겹친 지점에서 text-block ↔ asset-block **순환** |
| 8 | 드롭 대상 도달성 | ✅ | pull 0/−60/−140/−260/−400 전부 **7/7 도달, 도달불가 0** |
| 9 | undo / redo | ✅ | −180px → undo 빔 → redo −180px |
| 10 | ⌘↑ 순서 이동 | ⚪ | 합성 호출로는 미동작 — **겹침 0에서도 동일** ⇒ 회귀 아님(선택 판정 문제) |

## 미해결 — 현빈 결정 필요
**Figma JSON · Design JSON 은 겹침을 싣지 못한다.** `export-figma-json.js:889` 가 저장 문자열을
DOMParser 로 «오프라인 파싱»해 순회하고 섹션 객체가 `{id,name,background,bgImage,blocks}` 뿐이라
y·height 축이 없다(:881). z-index·marginTop 참조 각각 0회. Design JSON 도 의미 모델이라 좌표축 없음.
⇒ ⒜「화면·저장·HTML·PNG 전용, JSON 2종은 겹침 없는 근사」로 명시하고 간다  또는
  ⒝ 스키마에 pullUp 축 추가 + Figma 플러그인 빌더 수정(별건).

## 잠복 지뢰(이번 건 무관, 싸게 막을 수 있음)
`banner02-block.js:106` · `comparison-block.js:151` 이 `block.style.margin` **단축속성**을 쓴다.
지금은 두 블록이 항상 `.row` 로 감싸여 겹침 단위가 row 라 안 터지지만, 래핑이 깨지면
저장과 PNG 가 «동시에» 조용히 무너진다. `marginLeft/marginRight` 로 바꿔두는 게 싸다.

---

# 2차 — 새 프로젝트 + 실제 작업물 적대 QA (2026-09-04, dev `203701c` 이후)

시험 프로젝트: **`proj_1788486198087`(새로 만든 백지)** · **`proj_qa1788486917`(실제 작업물 80MB 사본)**
★원본 `proj_1786073068344` 는 **무접촉** — sha256 앞 16 `2cea213a35c84e5b` 시험 전후 동일.
★내보내기는 `exportSection(sec,'png',w,{returnDataUrl:true})`(export-image.js:607) 로 뽑아
  **파일·대화상자 0**. 다운로드 폴더 오염 없음.
★앱 창을 앞으로 가져오지 않았다(현빈이 다른 작업 중).

## 입력 방어
| 입력 | 결과 |
|---|---|
| −99999 / +99999 | −600(FLOOR) / 0 ✅ |
| NaN · undefined | 0 으로 안전 낙하 ✅ |
| 문자열 "-50" | −50 ✅ |
| 소수 −37.6 | −38(반올림) ✅ |
| 갭 블록 | 안 걸림 ✅ (★1차엔 걸렸다 — `fe7cc27` 로 수정) |
| 섹션 자체 | 안 걸림 ✅ |
| 0 복귀 | margin·position·z·dataset **전부 빔** ✅ |

## 산출 경로
| 경로 | 결과 |
|---|---|
| 디스크 저장본 | `margin-top:-120px` · `z-index:85` 실림 ✅ |
| 재로드 | 인라인·position·z 복원 + 배지 재부착 ✅ |
| HTML 내보내기(클론→innerHTML) | margin-top·z-index:85·position:relative 셋 다 실림 ✅ |
| PNG 내보내기 | 아래 픽셀 대조 참조 |

## ★픽셀 대조 — 「겹침 부분 외에는 안 건드리나」
**백지 프로젝트**(겹침 −120): 높이 1120→1000(정확히 120 감소).
  겹침 위쪽 0~254행 **다른 픽셀 0개**. 나머지 차이는 **최대 1~2 계조** = 그라데이션 디더링 노이즈.

**실제 작업물 사본**(23섹션·194블록, 겹침 −80): 높이 2202→2122(정확히 80 감소).
  **1565행 위쪽 픽셀 하나도 안 다름.** 변화는 겹침 지점 아래에만. 육안 대조도 의도대로.

## 섹션 합치기(⌘M) × 겹침
겹침 −90 준 블록이 든 B섹션을 A에 ⌘M 합치기 →
· 섹션 2→1 · 단위 생존 · `margin-top:-90px`·`z:85`·`relative` **그대로**
· 부모가 `.section-merged-part` 로 바뀌어도 `eligible` **true** · `getPull` −90
· 합친 뒤 패널 정상 표시(min −600, 값 −90, 「초기화」 보임)
· ★PGE 가 지적했던 「슬라이더를 누르기만 해도 값이 잘린다」 **안 일어남**(−90 유지)
· 극단 클램프도 섹션 상단을 안 넘음

## 이번 QA 로 잡은 결함 2
1. **`setPull` 이 «걸 수 없는 자리»를 안 걸렀다** — 갭 블록에 −60 이 그대로 먹었다(`fe7cc27`).
2. **드래그 좌표 보정이 오히려 나빴다** — 되돌렸다(`55eb895`). 실측 대조표는 그 커밋 메시지에.
