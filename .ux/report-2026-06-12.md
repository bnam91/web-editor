# GODITOR UX 감사 리포트 — 2026-06-12 (ux-squad 엔진 v3.0 첫 RUN)

**스코프**: 전 표면 × 6차원 (layout/semantics/flow/feedback/error/onboarding) · 13영역 · method=CDP 실주행+정적 전수
**판정**: 발견 53건 = judgment 39 + conformance 확정 13·의심 1 · 고등급 후보 14건 전건 독립 검증자 적대적 반증 → **확정 11 / 기각 3**
**by_adjudication**: self 40 / central-confirmed 11 / central-refuted 3 (작성자 본인이 닫은 고등급 0 — 불변식 준수)
**게이트 컨텍스트**: 페르소나=외부 일반 사용자까지(G1) · 기준값 뎁스≤4/지연≤300ms/첫산출≤7(G3, 첫 사이클 느슨) · PM 패널 독자 스타일=의도된 예외(G5①)

---

## 헤드라인 — 좋은 뼈대, 숨은 손잡이

**측정 가능한 것은 전부 합격이다**: 클릭 뎁스 위반 0 (4플로우 실측 2~4), 피드백 지연 위반 0 (실측 19~96ms vs 기준 300), 첫 산출물 2단계 (기준 7). GODITOR의 속도와 진입 동선은 외부 사용자 기준으로도 우수하다.

**문제는 전부 "보이지 않는 것"에 있다**: 파괴 액션의 무보호(blocker 1), 핵심 액션·복구 경로의 비가시(키보드 전용 은닉), 신호의 진실성('저장됨' 거짓 표시), 그리고 이름 없는 산출물(Untitled 9/20). 수치 기준으로는 안 잡히고 judgment 트랙이 잡은 것들 — 이 스쿼드를 judgment-native로 설계한 이유가 첫 RUN에서 입증됐다.

---

## 우선 검토 Top 10 (확정만 — 승인 시트는 HTML에서)

| # | ID | 등급 | 내용 | 클러스터 |
|---|----|----|------|---------|
| 1 | ERR-C1 | **blocker** | **페이지 삭제 완전 무보호** — confirm·Undo·복구 진입로 전무. 검증에서 악화: 렌더러의 페이지 수 감소 가드(save-load.js:131-135)는 main이 더 이상 반환하지 않는 **죽은 코드**(BUG-44로 가드 제거됨). 어떤 계층도 못 막고 못 되돌림 | 무보호 파괴 |
| 2 | UXS-flow-001 | high | 섹션 이동·복제·삭제 **가시 진입로 전무** — 전부 키보드/레이어 드래그 은닉. 검증: .tb-hidden-mvp(의도 숨김) 아님 — 주석에 약속된 컨텍스트 메뉴 미구현 확정 | hidden-affordances |
| 3 | UXS-flow-002 | high | 섹션 삭제 즉시 실행 — 확인·토스트·Undo 힌트 전무 (Undo는 작동하나 지각 불가) | hidden-affordances |
| 4 | ERR-J1 | high | Undo가 Cmd+Z 전용 — history.js가 갱신하는 `#undo-btn`이 index.html에 없음(데드 코드). 외부 사용자에게 복구 경로 비가시 → 캔버스 삭제군의 "Undo 있으니 합격" 근거가 체감상 붕괴 | hidden-affordances |
| 5 | FB-J1 | high | **'저장됨' 거짓 신호** — 파일 쓰기 완료(await) 전에 표시, 실패 시 '저장됨'+'❌' 토스트 모순 신호 (save-load.js) | save-signal |
| 6 | UXO-J02 | high | 프로젝트 생성 시 이름 안 묻고 rename은 숨은 더블클릭뿐 → **실저장소 20개 중 9개 Untitled*** (검증자 재집계 확정, "Untitled_두더지?" 같은 식별 실패 실증) | hidden-affordances |
| 7 | RC-export | high(머지) | **Export 메뉴 정체성 혼란** — UXS-layout-001 + UXS-semantics-009 두 차원 독립 수렴: 'Export' 드롭다운 7항목 중 5개가 비-내보내기(불러오기/저장/Figma/참고 이미지), 정작 섹션 PNG 내보내기는 props에 분산(UXS-flow-008 관찰 병합). 수정 1건(메뉴 재명명·재편성)으로 함께 해소 | root-cause |
| 8 | ERR-C2 | major | 템플릿 삭제 무확인 영구 파괴 (진입로 2곳) | 무보호 파괴 |
| 9 | ERR-C3 | major | 템플릿 덮어쓰기 무확인 | 무보호 파괴 |
| 10 | UXS-layout-002 | high→**medium 조정** | 섹션 props 내 primary 파랑 2개 경쟁 — Template '저장'이 프로젝트 저장으로 오인. 검증: "빈 이름 템플릿" 시나리오는 이름 가드(prop-section.js:550-551) 실재로 사실 오류 → 하향. 오인 배치 자체는 confirmed | save-signal |

## Root-cause 머지 (수정 1건으로 함께 해소)

| 키 | 묶인 발견 | 상류 원인 | 수정 방향 |
|---|---|---|---|
| **terminology-glossary-missing** | semantics 6건 (스크래치패드 4종 표기 · '컴포넌트' 3의미 · Label/라벨 · Row·Column/행·열 · '프레임' 이중 referent · 경어체 6:31) | **용어 사전 부재** — design MATRIX의 "토큰 패밀리 부재"와 동형 패턴 | `.ux/profile.md` 용어 사전 작성(정용어 확정은 현빈) → 이후 사전 위반은 deterministic 자동 판정 |
| **RC-export** | UXS-layout-001 + UXS-semantics-009 (+flow-008 관찰) | 'Export' 버튼이 실질 파일 메뉴인데 Export로 명명·구성 | 메뉴 재명명(예: '파일') 또는 내보내기 전용으로 재편성 — 1건 |
| **hidden-affordances** (클러스터 — 수정 2건) | UXS-flow-001/002 + ERR-J1 (+UXO-J02 인접) | 핵심 액션·복구가 전부 비가시 채널(키보드·더블클릭·드래그)에만 존재 — "보이는 손잡이" 계층 부재 | PR-a: 섹션 컨텍스트 메뉴/툴바(이동·복제·삭제+삭제 피드백) · PR-b: undo 버튼 UI 배치 |
| **save-signal** (클러스터 — 수정 별개) | FB-J1 + UXS-layout-002 + UXO-J03(medium) | 자동저장 체제에서 '저장' 신호·레이블 체계 미정리 | FB-J1은 await 코드 수정, 레이블은 'Template으로 저장' 등 명시화 |

## 기각된 후보 3건 (적대적 검증의 수확)

- **UXS-flow-003** (silent no-op): 미선택 가드 `showNoSelectionHint()`가 6개+ 경로에 실재 + **같은 RUN의 U-feedback이 동일 경로를 3회 실측**(토스트+shake 26.3ms) — 차원 간 교차 증거로 기각. 단 `_activeFrame` 잔존 시 silent return하는 협소 경로(block-factory.js:402)는 실재 → suspected/minor로 분리 기록.
- **UXS-semantics-001** ('Canvas 블록' 유령 레이블) · **ERR-C4** (컴포넌트 삭제 무확인): 코드 결함은 실재하나 진입로가 HTML 주석 봉인(index.html:628-638)·SEALED 콜사이트 0건 — **도달 불가 휴면 표면**. 현재 UX 위반 아님, **재활성화 시 재발행 조건** 부착 (GATE에서 감시).

## 영역별 결산 (13영역 전원 — 침묵 없음)

- **발견 집중**: toolbar(Export 메뉴·Preview 최강조), canvas-block(섹션 액션 은닉·삭제 무피드백), prop-panel(primary 경쟁·generic 레이블), home-pages(Untitled·rename 은닉), modals/template-browser(무확인 파괴)
- **클린**: graph(파괴 액션 0 확인), pickers(states 모범 — layout은 스킵 사유 명기), design-system(treated, 컨트롤 자체 양호)
- **스킵(정직 보고)**: pm-panel(G5 예외 + 미진입), license.html admin 구간(외부 사용자 진짜 첫 관문 — **다음 사이클 1순위 구멍**), 네이티브 파일 다이얼로그(UNMEASURED)
- 상세는 각 `results/ux-*.json`의 area_summary — HTML 결산판이 합산 뷰.

## 측정 기록 (conformance — 전부 PASS)

| 측정 | 실측 | 기준 |
|---|---|---|
| 플로우 뎁스 | edit-block 4(경계) · insert-asset 3 · section-rearrange ≤2 · export-publish 2~3 | ≤4 |
| 피드백 지연 (3회 중앙값) | prop 변경 40.2ms · 블록 추가 95.9ms · 저장 표시 18.7ms · 실패 토스트 26.3ms | ≤300ms |
| 첫 산출물 | 2단계 | ≤7 |

- flows/edit-block.md 기대 뎁스 3→실측 4 — **명세 갱신 제안** (드롭다운 2클릭 + 추가 후 미선택이 +1뎁스 원인. 사용자 승인 후 flows 갱신).

## 프로세스 메모

- **레드팀 미발동**: 자동 발동 조건(전 영역 clean + 고등급 0) 미해당 — 고등급 11건 확정으로 1차 그물 검출력은 입증. 다음 사이클(또는 MATRIX) 때 투입 권고. *(no silent caps — 명시적 스킵 기록)*
- **에이전트 간 창 경합**: 6에이전트가 단일 Electron 창 공유 → 페이지 전환 간섭 수회, 앱 2회 재기동(.env iCloud dataless는 brctl로 해결). 오염 배치는 각 에이전트가 폐기·재측정. **다음 RUN 개선**: 실주행 차원(flow/feedback/error/onboarding)은 순차, 정적 차원만 병렬.
- **스키마 일탈**: 3개 에이전트가 adjudication에 enum 대신 산문 기입 → 취합 불변식이 잡아 강제 재처리(14건 전건 검증 통과). 산문은 `self_guard_note`로 보존. 엔진 프롬프트에 enum 강조 필요 — UPGRADE 백로그.
- **out_of_scope 채널 25건**: license.html 구간(격자 구멍 후보 1순위), Export 흐름-배치 교차 메모, MATRIX 이관 항목 등 — 다음 PROFILE 갱신 입력.

## 정리 대기 (사용자 승인 후)

1. 테스트 프로젝트 `proj_1781188552759` ("ux-squad-test-0611") — onboarding 측정용, 삭제 승인 필요
2. 테스트 프로젝트(proj_1775644888754)에 U-flow 테스트 블록 3개 잔존
3. `~/Downloads`에 export-publish 실주행 부산물 JSON 3·PNG 4
4. flows/edit-block.md 기대 뎁스 3→4 갱신

---
*정본: 이 파일 + results/ux-*.json + results/verification-2026-06-11.json · HTML은 재생성 가능한 뷰 · FIX는 HTML 승인 시트 내보내기(approved)만 입력으로 진입*
