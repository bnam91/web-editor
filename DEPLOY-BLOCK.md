# ⛔ dev latest 승격 금지

**사유: C8 (협업 undo가 상대 작업 삭제) 미수정.**

협업 중 A가 ⌘Z를 누르면, A의 undo가 되돌린 상태가 재전파돼 **상대(B)의 디스크에서 B 자신의 작업이 영구 소실**된다 — 경고도 흔적도 없어 B는 왜 사라졌는지 모르고, A도 「자기 걸 되돌렸을 뿐」이라 모른다. 두 인스턴스 + 서버 타임라인으로 실측 확정(`BUGS.md` C8 항목, `runs/c8`).

> ⚠️ 확정 치명 4건(C2-A9·B1·N2·C7)은 수정·검증 완료됐으나 **C8은 미수정**이다.
> 「치명 4건 고쳤다」를 「이제 배포해도 된다」로 읽지 말 것 — 요약은 「하나만 다른 것」을 제일 잘 숨긴다.

## 해제 조건 (둘 중 하나)

- ⒜ `feat/collab-undo-op` 브랜치(정통 오퍼레이션 기반 undo) 완성·검증·머지 — **또는**
- ⒝ 협업 임시조치(협업 기능 차단) 적용.

둘 중 하나를 충족한 뒤 **이 파일을 삭제**하면 `tools/deploy-gate.js` 게이트가 열려 release/publish 가 진행된다.

—
작성: 지디, 2026-08-15. 원인 근거: `BUGS.md` C8.
